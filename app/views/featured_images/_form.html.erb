<%= form_for [@image, @featured_image], :html => {:class => 'featured_image'} do |f| %>
  <%= image_tag @image.picture.url(:thumb) %>

  <fieldset>
    <%= f.label :feature_at %>
    <%= f.datetime_select :feature_at, {}, :class => 'datetime' %>
  </fieldset>
  <%= f.submit 'Save' %>
<% end %>
